# Production transport test artifacts — not committed
production_store/
production_store_live/
scale_validation_store/blobs/

# E2E store: regenerate via pipeline (real official sources)
# The store JSONL is real evidence — committed. Binary blobs (raw HTML
# retrieved from official sources) are gitignored (they can be
# re-fetched by re-running the E2E pipeline).
e2e_store/blobs/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS
.DS_Store
Thumbs.db
scale_validation_store/blobs/

# V2 — ignore transient test artifacts (kept locally for verification)
/concurrent_ingestion_results.json
/transport_load_results.json
/corpus_expansion_results.json
/golden_corpus_frozen.json

# V1-Real — ignore transient test artifacts (kept locally for verification)
/real_corpus_parallel_results.json
/real_corpus_expansion_results.json
